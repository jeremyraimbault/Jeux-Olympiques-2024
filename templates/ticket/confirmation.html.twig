{% extends 'base.html.twig' %}

{% block title %}Confirmation d'achat{% endblock %}

{% block body %}
<div class="container my-5">
    <h1 class="mb-4">🎉 Merci pour votre achat !</h1>

    <div class="row g-4">
        {% for ticket in tickets %}
            <div class="col-md-6 col-lg-4">
                <div class="card shadow-sm h-100">
                    <div class="card-body">
                        <h5 class="card-title">Offre : {{ ticket.offer.name }}</h5>
                        <p class="card-text">Date : {{ ticket.createdAt|date('d/m/Y H:i') }}</p>
                    </div>
                </div>
            </div>
        {% else %}
            <p>Aucun ticket acheté.</p>
        {% endfor %}
    </div>

    <div class="d-flex justify-content-between mt-4">
        <a href="{{ path('app_offer_index') }}" class="btn btn-primary">
            <i class="bi bi-arrow-left"></i> Retour à la boutique
        </a>
        <a href="{{ path('ticket_list') }}" class="btn btn-primary">
            Retour aux tickets <i class="bi bi-arrow-right"></i>
        </a>
    </div>
</div>
{% endblock %}
