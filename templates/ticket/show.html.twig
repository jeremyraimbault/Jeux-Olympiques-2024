{% extends 'base.html.twig' %}

{% block title %}Votre e-ticket{% endblock %}

{% block body %}
<div class="container my-5">
    <h1 class="mb-4">E-ticket pour <strong>{{ ticket.offer.name }}</strong></h1>
    <p><strong>Date d'achat :</strong> {{ ticket.createdAt|date('d/m/Y H:i') }}</p>

    <div class="my-4">
        <h3>QR Code</h3>
        <img src="{{ qrCode }}" alt="QR Code du billet" class="img-fluid border rounded" style="max-width: 300px;">
    </div>

    <a href="{{ path('ticket_list') }}" class="btn btn-primary mt-3">
        <i class="bi bi-arrow-left"></i> Retour à mes tickets
    </a>
</div>
{% endblock %}
