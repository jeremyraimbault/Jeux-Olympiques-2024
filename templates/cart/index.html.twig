{% extends 'base.html.twig' %}

{% block title %}Mon Panier{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<h1>🛒 Mon panier</h1>

<div class="example-wrapper">
    {% if items is empty %}
        <p>Aucune offre dans le panier.</p>
        <a href="{{ path('app_offer_index') }}">Retourner à la boutique</a>
    {% else %}
        <table>
            <thead>
                <tr>
                    <th>Offre</th>
                    <th>Quantité</th>
                    <th>Prix unitaire</th>
                    <th>Sous-total</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {% for item in items %}
                    <tr>
                        <td>{{ item.offer.name }}</td>
                        <td>{{ item.quantity }}</td>
                        <td>{{ item.offer.price }} €</td>
                        <td>{{ item.subtotal }} €</td>
                        <td>
                            <a href="{{ path('cart_remove', {id: item.offer.id}) }}">🗑️</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

        <h3>Total : {{ total }} €</h3>

        <a href="{{ path('ticket_checkout')}}" class="btn btn-success">Réserver</a>
        <a href="{{ path('app_offer_index') }}">Retourner à la boutique</a>
    {% endif %}
</div>
{% endblock %}
